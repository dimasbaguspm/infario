# Nginx config for static type
limit_req_zone $binary_remote_addr zone=versaur:10m rate=20r/s;

# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name versaur.dimasbaguspm.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name versaur.dimasbaguspm.com;

    ssl_certificate     /etc/letsencrypt/live/versaur.dimasbaguspm.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/versaur.dimasbaguspm.com/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    limit_req zone=versaur burst=40 nodelay;

    root /deployments/versaur/storybook-static;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
